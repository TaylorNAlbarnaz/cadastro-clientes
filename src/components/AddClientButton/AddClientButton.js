import './AddClientButton.css';
import Button from'react-bootstrap/Button';

function AddClientButton() {
  return (
    <>
      <Button>AddClientButton</Button>
    </>
  );
}

export default AddClientButton;
